//declare una funcion que puede resultar dos tipos de datos
function incognito(x:string | number) {
    //cuando declaras un valor aun no es definido no recibo metodos
    // console.log(`error ${x.toLowerCase()}`);
    // console.log(toFixed());
}
incognito("hola")